generator client {
  provider        = "prisma-client-js"
  previewFeatures = ["fullTextSearchPostgres", "fullTextSearch"]
  output          = "../../.generated"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}
